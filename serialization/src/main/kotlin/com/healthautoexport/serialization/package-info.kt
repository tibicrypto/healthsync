/*
 * Source root for the :serialization module.
 *
 * Holds the pure-JVM JSON/CSV/GPX serializers and parsers implemented in
 * tasks 3–5. This placeholder establishes the Kotlin source directory so the
 * module compiles cleanly before those types exist.
 */
package com.healthautoexport.serialization
