/*
 * Source root for the :data module.
 *
 * Holds the Android framework adapters (Health Connect / Huawei data sources,
 * Room, DataStore, EncryptedSharedPreferences, the six Destinations, and the
 * WorkManager scheduler) implemented in tasks 13–20. This placeholder
 * establishes the Kotlin source directory before those types exist.
 */
package com.healthautoexport.data
